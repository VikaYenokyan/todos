mixin fetchBlock(mods)
  -
    const baseName = 'fetchBlock'
    let allMods = ''
    if (typeof mods !== 'undefined' && mods) {
      const modsList = mods.split(',')
      for (const element of modsList) {
        allMods += element.trim()[0] === '_' ? ' ' + baseName + element.trim() : ' ' + element.trim() + '__' + baseName
      }
      allMods = allMods.trim()
    }

  .fetchBlock(class=allMods)&attributes(attributes)
    h1 Клиент-серверное взаимодействие
    form.load-todo-form(action="")
      h2 Поиск поста по ID
      p 
        label(for="todo-id") Идентификатор задачи
        input(id="todo-id" type="number" value="1")
      button(type='submit') Загрузить информацию с сервера

    .result
